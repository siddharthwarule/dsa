package sorting;

//bubble sort in java
//in that the real world phenomena of bubble's are used bubbles are come at 
// the surface in that we can push the wighted element into the last 
//time complexity = On^2;
public class bubbleSort {

    public static void main(String[] args) {


        // for (int i = 0; i < arr.length - 1; i++){

        //     for (int j = 0; j < arr.length - 1 - i; j++){

        //         if (arr[j] >= arr[j+1] ) {
        
        //             //swap 
        //             int temp = arr[j];

        //             arr[j] = arr[j+1];

        //             arr[j+1] = temp;

        //         }

        //     }

        // }

        // for(int i = 0 ; i < arr.length; i++){

        //     System.out.println(arr[i]);

        // }


        // for(int i = 0 ; i < arr.length -1 ; i++){

        //     for(int j = 0 ; j < arr.length - 1; j++){

        //         if (arr[j] >= arr[j+1] ) {
                    
        //             int temp = arr[j];

        //             arr[j] = arr[j+1];

        //             arr[j+1] = temp;

        //         }

            // }

        // }


        // for(int i = 0 ; i < arr.length-1; i++){

        //     for (int j = 0 ; j < arr.length-i-1; j++){

        //           if (arr[j] >= arr[j+1]) {
                    
        //               int temp = arr[j+1];

        //               arr[j+1] = arr[j];

        //               arr[j] = temp;
                
        //           }

        //      }

        // }

    

        // for(int i = 0 ; i < arr.length-1; i++){

        //     for(int j = 0 ; j < arr.length-i-1; j++){

        //         if (arr[j] >= arr[j+1]) {
                    
        //             int temp =  arr[j];

        //             arr[j] = arr[j+1] ;
                    
        //             arr[j+1] = temp ; 

        //         }

        //     }

        // }


        // for(int i = 0 ; i < arr.length-1; i++ ){

        //      int smallerIndex = i ; 

        //     for(int j = i+1 ; j < arr.length ; j++){

        //      if (arr[j] < arr[smallerIndex]) {

        //            smallerIndex = j ; 
               
        //         }
        //     }

        //         int temp = arr[smallerIndex];

        //         arr[smallerIndex]  =  arr[i];

        //         arr[i] = temp ;

        // }



        int arr[] = { 2, 9, 8, 7, 3, 4 ,5,7};


        for(int i = 1 ; i < arr.length; i++){

            // int j  =  arr[]

            

        }


    


    for(int i = 0 ; i < arr.length; i++){

        System.out.println(arr[i]);

    }



    }



}
